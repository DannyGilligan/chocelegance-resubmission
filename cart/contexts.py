

def cart_contents(request):
    """
    This function returns a dictionary and stores it in the
    context variable, this context can then be accessed by
    all templates across the entire site
    """

    context = {
        
    }

    return context